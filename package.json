{
  "name": "chatinterface-classifier",
  "version": "1.0.0",
  "description": "Backend classification engine — maps user messages to inferences and actions via Gemini",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
